function f(input):
    Mark the first line as the leader
    for every line in input:
        if this line is Jump:
            Mark the next line as the leader
            Mark the target as the leader
    
    Every subinterval between each leader is Basic Blocks



